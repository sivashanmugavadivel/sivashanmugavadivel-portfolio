# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local originals of compressed video previews (not deployed)
public/video-previews/_orig/

# This is a pnpm project — pnpm-lock.yaml is the lockfile, and CI installs
# with it. An npm lockfile alongside it resolves a different dependency tree
# to the one that actually ships, so keep it out of the repo.
package-lock.json
yarn.lock
